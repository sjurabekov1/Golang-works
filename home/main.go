package main

import "fmt"

type Talaba struct {
	name string
	age uint8
	scholar int
	results []int
}

func main() {
	var n int
	stud = Talaba{}
	fmt.Println("Talabalar sonini kiriting: ")
	fmt.Scan(&n)
	for i := 0; i < n; i++ {
		fmt.Scan(&stud.name)

	}
}